This folder is reserved for reusable React components.

For this no-build setup, the production UI is currently implemented in `frontend/src/App.js`
so it can run directly with Babel in `frontend/index.html`.
